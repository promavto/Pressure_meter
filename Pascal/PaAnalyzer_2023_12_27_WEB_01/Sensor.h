#pragma once
#include <Arduino.h>
//-------------------------------------------------------------------------------------------------------------------------------------------------------------------------
typedef struct
{
  bool HasValue;
  uint8_t Value;
  uint8_t Fract;
  
} SensorData;
//-------------------------------------------------------------------------------------------------------------------------------------------------------------------------
class SensorClass
{
  public:
      SensorClass();

      void setup();
      uint16_t read();
	  uint16_t readRef();
      SensorData compute();
      int reference_voltage = 0;
};
//-------------------------------------------------------------------------------------------------------------------------------------------------------------------------
extern SensorClass Sensor;

